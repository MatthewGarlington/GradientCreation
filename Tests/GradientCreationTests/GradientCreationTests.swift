import XCTest
@testable import GradientCreation

final class GradientCreationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GradientCreation().text, "Hello, World!")
    }
}
